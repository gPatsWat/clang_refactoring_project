#include <iostream>
#include <benchmark/benchmark.h>

void foo() {
    int a = 1, b = 0;
    if(a) {
        b++;
    }
    else {
        b += 2;
    }
}

int main(int argc, char** argv) {
    int num
    for(int i = 0;i < stoi(); )
}